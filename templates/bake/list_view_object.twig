{{ element('Bake.file_header', {
    namespace: "#{namespace}\\Seo",
    classImports: [
        'SeoBakery\\Core\\SeoAwareInterface',
        'SeoBakery\\Core\\SeoAwareListViewObject',
    ],
}) }}

/**
* {{ name }} ListView Object
*/
class {{ name }}ListViewObject extends SeoAwareListViewObject implements SeoAwareInterface
{
    /**
    * Returns the table name
    * i.e. `Products`, 'Articles`
    *
    * @return string
    */
    protected function getTableName(): string
    {
        return '{{ name }}';
    }
}
