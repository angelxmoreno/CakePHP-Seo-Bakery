{{ element('Bake.file_header', {
    namespace: "#{namespace}\\Seo",
    classImports: [
        'SeoBakery\\Core\\SeoAwareInterface',
        'SeoBakery\\Core\\SeoAwarePageObject',
    ],
}) }}

/**
* {{ name }} Page Object
*/
class {{ name }}PageObject extends SeoAwarePageObject implements SeoAwareInterface
{
/**
* returns the template path for the Pages template
* i.e. /profile/about or /contact
*
* @return string
*/
public function getTemplateName(): string
{
return '{{ path }}';
}
}
